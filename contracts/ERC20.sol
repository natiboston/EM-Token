pragma solidity ^0.5;

import "./Compliant.sol";

contract ERC20 is Compliant {

    // Data structures (in eternal storage)

    // Events

    event Transfer(address indexed from, address indexed to, uint256 value);
    event Minted(address account, uint256 value);
    event Burned(address account, uint256 value);

    // Constructor

    // Modifiers

    // Interface functions

    // Internal functions

    // Private functions
    
}
